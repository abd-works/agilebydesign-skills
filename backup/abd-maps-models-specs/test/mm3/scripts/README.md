# Workspace-local scripts (`test/mm3`)

Put **this-workspace-only** automation here (e.g. Mutants & Masterminds / Heroes Handbook normalization, one-off transforms tied to this corpus).

Do **not** add those entry points under the skill package `skills/abd-maps-models-specs/scripts/` — that tree is for tooling shared across all workspaces that use this skill.

Run scripts from the skill package root with Python path or explicit paths as you prefer; this folder is not on `PYTHONPATH` by default.
