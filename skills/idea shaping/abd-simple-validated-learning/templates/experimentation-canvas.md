# Experimentation canvas — single experiment (template)

Use for **one** high-stakes or ambiguous belief when a **full backlog row** is not enough surface area for the room. Pair with `validated-learning-backlog.md` for the same engagement so the experiment stays on the **prioritised** list.

---

## Experiment

**Id / label:** `{{EXPERIMENT_ID}}`  
**Trace to:** (canvas row, assumption #, or doc) `{{TRACE}}`

### Belief we are testing

*We used to think … / We believe …*  
`{{BELIEF}}`

### Hypothesis (falsifiable)

*We expect that …*  
*We will be wrong if …*  
`{{HYPOTHESIS}}`

### Method (Plan — smallest that could change our mind)

- **Activity:** `{{what}}`  
- **Timebox / scope:** `{{when and how much}}`  
- **Cohort / sample:** `{{who or what data}}`  
- **Ethics / compliance note (if any):** `{{...}}`

### What we will measure (Validate)

| Signal | How captured | Pass / strong signal | Fail / refute |
| --- | --- | --- | --- |
| `{{...}}` | `{{...}}` | `{{...}}` | `{{...}}` |

### Owner and date

- **Accountable:** `{{NAME}}`  
- **Target date:** `{{DATE}}`  
- **Participants / SMEs:** `{{...}}`

### Learn (after)

- **Result:** (evidence, one screen or link) `{{...}}`  
- **Decision:** (refine canvas / run bigger test / stop / proceed) `{{...}}`  
- **Update these artifacts:** `{{opportunity canvas section, map, story items, etc.}}`

---

*Discovery / cheap validation (research, interviews, paper, pilot) fits this canvas. A **shipped** increment with full analytics is a **product** learning loop; still valid, but not the default emphasis of **abd-simple-validated-learning**.*
