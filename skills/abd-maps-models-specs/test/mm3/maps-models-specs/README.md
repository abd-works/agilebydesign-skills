# Maps / models / specs (test fixture)

- **`map-model-spec.md`** — **Stub** for a future **builder candidate**. The default **evaluator** does **not** use this file as the rubric.
- **Ontology / full OO write-up** — moved to **`docs/reference/`** so scripts in the default loop do not read the target solution.

See `docs/reference/README.md` and `docs/orchestrator.md` for role separation (builder vs evaluator vs orchestrator).
