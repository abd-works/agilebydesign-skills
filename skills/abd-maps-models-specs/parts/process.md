# Process — Map-Model-Spec Synthesizer

Pipeline: Context → Discover → Deepen → Canonicalize → Evidence → Structure → Finalize.

**Core principle:** Discover taxonomy layer by layer, top-down, with evidence indexed as you go. Each step is a separate AI or code pass. Scanners enforce structure mechanically. AI resolves violations — scanners never propose fixes.

**Two parallel artifacts produced at every step:**

- **Domain model** — modules, concepts, properties, operations (what things are and own)
- **Story map** — epics, sub-epics, stories, acceptance criteria, specifications, examples (what actors do and what changes)

These are two views of the same coin and must be produced simultaneously.

**Output files (single evolving document):**

- `map-model-spec.json` — forward index (Step 1 creates; Step 2 deepens; Step 3 canonicalizes; Step 5 structures)
- `map-model-spec.md` — human-readable summary
- `mms-chunk-index.json` — reverse index (generated by `mms_build_chunk_index.py`)

---

## Corpus Coverage

**AI steps (1–3):** Step 1 samples 30% of chunks (stratified: beginning, middle, end) for orientation. Step 2 reads only chunks indexed by Step 1 (identified, provisional, deferred). Step 3 uses the index; no new chunk reads.

**Code step (4):** Evidence extraction scans the **full corpus** (`context_chunks.json`). This is where coverage expands beyond 30%. Code mines all chunks for actions, decisions, states, and relationships guided by the concept list. Evidence feeds into Step 5.

**mms-chunk-index.json** reflects chunks the AI has cited in map-model-spec.json. It is for AI lookup in Steps 2–3. Full corpus coverage comes from Step 4 evidence extraction, not from this index.

---

## Discover


| #   | Step                             | Actor | What it does                                                                                                                                         | Ref                                    | Outputs                                                                 |
| --- | -------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------- |
| 1   | **Shape- Modules and Epics**     | AI    | Orient in corpus; discover module/epic pairs; sketch concepts; 2–3 confirming story names per epic                                                   | [step1-discover.md](step1-discover.md) | map-model-spec.json, map-model-spec.md                                  |
| 2   | **Explore Concepts and Stories** | AI    | Per module/epic: full concepts (properties, operations, invariants); full stories (Trigger, Response, Pre-Condition, Failure-Modes); resolve [defer] | [step2-deepen.md](step2-deepen.md)     | map-model-spec.json (deepened), map-model-spec.md, mms-chunk-index.json |


---

## Stage 2: Canonicalize (Step 3)


| #   | Step                            | Actor | What it does                                                                                           | Ref                                            | Outputs                                            |
| --- | ------------------------------- | ----- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------- | -------------------------------------------------- |
| 3   | **Canonicalize Across Modules** | AI    | Unify naming; wire cross-module relationships; resolve cross-cutting concerns; finalize subtypes/enums | [step3-canonicalize.md](step3-canonicalize.md) | map-model-spec.json (canonical), map-model-spec.md |


---

## Stage 3: Evidence and Structure (Steps 4–5)


| #   | Step                    | Actor | What it does                                                                                                 | Ref                                      | Outputs                                             |
| --- | ----------------------- | ----- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------- | --------------------------------------------------- |
| 4   | **Evidence Extraction** | Code  | Mine chunks for actions, decisions, states, relationships; guided by map-model-spec.json                     | [step4-evidence.md](step4-evidence.md)   | evidence/*.json                                     |
| 5   | **Structure**           | AI    | Build full model from canonical scaffold + evidence: properties, operations, story links, decision ownership | [step5-structure.md](step5-structure.md) | map-model-spec.json (structured), map-model-spec.md |


---

## Stage 4: Finalize (Step 6)


| #   | Step                                                   | Actor | What it does                                                                                                             | Ref                                    | Outputs                     |
| --- | ------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------- | --------------------------- |
| 6   | **Behavior, Variation, Consolidate, Assess, Finalize** | AI    | Assign operations; link behaviors to steps; split stories by subtype; fix anti-patterns; assess; produce validated model | [step6-finalize.md](step6-finalize.md) | map-model-spec.json (final) |


---

## Scripts


| Script                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mms_build_chunk_index.py` | Build mms-chunk-index.json from map-model-spec.json                                                                                                                                                                                                                                                                                                                                                                                   |
| `mms_scan_*.py`            | Run scanners. **Step 1–2:** chunks-referenced, no-duplicates, epic-confirming-stories, no-junk-concepts, concepts-have-owns, stories-have-trigger-response, domain-story-sync, hierarchy-sizing. **Step 3:** + cross-cutting-resolved. **Step 4:** evidence-files-exist, evidence-scaffold-refs, evidence-schema. **Step 5–6:** structural scanners (no-duplicates, domain-sync, hierarchy, concepts-owns, stories-trigger-response). |


---

## Config (solution modeler pattern)

**Skill config** `conf/abd-config.json` — one key:
- `solution_workspace` — path to workspace root (e.g. `mm3`). When set, all paths resolve relative to workspace.

**Workspace config** `<workspace>/solution.conf` — paths relative to workspace root:
- `output_dir` — where map-model-spec.json, evidence/, mms-chunk-index.json live (default: `solution` or e.g. `maps-models-specs`)
- `context_path` — where context_chunks.json and chunk .md files live (default: `output_dir/context`)

When no workspace is configured, scripts fall back to skill root (flat layout).

## Workspace Layout

**With workspace (e.g. mm3):**
```
mm3/
├── solution.conf          # output_dir, context_path
├── maps-models-specs/     # output_dir
│   ├── context/           # context_chunks.json, *.md chunks
│   ├── generated/         # junk_config.json
│   ├── evidence/          # actions.json, decisions.json, ...
│   ├── map-model-spec.json
│   ├── map-model-spec.md
│   └── mms-chunk-index.json
```

**Flat (no workspace):**
```
map-model-spec-synthesizer/
├── conf/abd-config.json   # optional: solution_workspace
├── parts/, rules/, scripts/
├── context/               # context_chunks.json (corpus)
├── evidence/
├── map-model-spec.json
├── map-model-spec.md
└── mms-chunk-index.json
```

