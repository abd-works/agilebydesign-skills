# test/fixture/

Place minimal skill trees here for use as test fixtures.

Each fixture is a stripped-down skill directory with only the files needed
to exercise a specific behaviour (a scanner, a build path, a rule check, etc.).

See `test/README.md` for conventions.
