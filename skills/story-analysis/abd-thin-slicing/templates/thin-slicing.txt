Thin slicing — incremental backlog — plain text template
(Thin-slicing / incremental backlog — matches thin-slicing.md.)

Use the SAME increment/story coverage as thin-slicing.md; this file is plain text only.

Product: <optional>

Spine vs optional: mandatory core flow vs alternates/enhancements/error depth — only spine stories belong in the smallest slice unless you annotate.

Increment 1: <marketable outcome name>
 Outcome: <one line>
  Slicing notes: <optional>
  Stories:
    - <verb-noun story>
    - <story>

Increment 2: <marketable outcome name>
  Outcome:
  Slicing notes:
  Stories:
    - <story>
    - <story>

---
Template reference only — do not copy the following into generated project files:
- Vertical end-to-end slices, not horizontal epic-complete layers.
- Business-value increment names; risk and integration proven early.
- Quality ramps: manual/stub/minimal first, then automation and depth.
- Optional vs spine: map/markers must reflect optional work, not fake sequencing.
- In paired .md files, *italics* for domain terms; .txt stays markdown-free.
