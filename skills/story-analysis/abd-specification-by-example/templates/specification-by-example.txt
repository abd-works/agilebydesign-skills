Specification by example (BDD scenarios) — plain text template
(Conventions from agile_bots story_bot behaviors/scenarios.)

Use the SAME scenario coverage as specification-by-example.md; this file is plain text only.
Scenarios use Given/When/Then with inline real values — no tables, no placeholder tokens.

Formatting convention: DOMAIN CONCEPT in caps or clear label, value in quotes or plain inline
(In the .md file, domain concept names are bold and values are italic.)

Story: <Verb-Noun Title>
Story type: user | system | technical
Sources / context: (pointer to source or conversation / workshop date)

---

TEMPLATE

Background (optional — only if 3+ scenarios share setup; Given/And only):
  Given a CONCEPT <value> is in state <state>
    And that CONCEPT <value> is related to CONCEPT <value> with ROLE <value>
    And that CONCEPT <value> has CONCEPT <value> enabled

Scenarios

Scenario 1: <outcome-oriented name>
  Given CONCEPT <value> owned by CONCEPT <value> with STATUS <value> is selected
  And the LIMIT for that CONCEPT is <value>
  When the CONCEPT <value> performs action with CONCEPT <value>
  Then the CONCEPT is marked as <outcome>
    And a REPORT is sent to <value> showing <value>

Scenario 2: <outcome-oriented name>
  Given CONCEPT <value> with STATUS <value> is selected
  When the CONCEPT <value> performs action with CONCEPT <value>
  Then the CONCEPT is <rejected outcome>

---

EXAMPLE

Story: Submit Wire Payment
Story type: user
Sources / context: Enterprise Payments Requirements v2, Ch. 3 pp. 12-14

---

Background:
  Given a USER Jane Doe is logged into ChannelOne 2.0
  And that USER Jane Doe is representing ENTERPRISE Acme Corp with ROLE Wire Operator
  And that ENTERPRISE Acme Corp has PAYMENT SERVICE wire enabled
  And that USER Jane Doe has ENTITLEMENT WirePayment.Create with ENTITLEMENT STATUS Granted

---

Scenarios

Scenario 1: Submit payment within limit - approved
  Given ACCOUNT Acme Operating owned by ENTERPRISE Acme Corp with ACTIVATION STATUS Active is selected
    And the TRANSACTIONAL LIMIT daily_wire for that Account is $500,000.00 USD
  When the USER Jane Doe enters a PAYMENT AMOUNT of $10,000.00 USD
  Then the PAYMENT AMOUNT $10,000.00 USD is validated against TRANSACTIONAL LIMIT $500,000.00 USD
    And the WIRE PAYMENT is marked as successful
    And a REPORT is sent to Jane Doe showing formatted display $10,000.00

Scenario 2: Submit payment over limit - rejected
  Given ACCOUNT Acme Operating owned by ENTERPRISE Acme Corp with ACTIVATION STATUS Active is selected
    And the TRANSACTIONAL LIMIT daily_wire for that Account is $500,000.00 USD
  When the USER Jane Doe enters a PAYMENT AMOUNT of $500,000.01 USD
  Then the WIRE PAYMENT is rejected
    And a REPORT is sent to Jane Doe showing validation status rejected

---
Template reference only — do not copy the following into generated project files:
- Given = state; first action is When; Then = domain-visible outcomes.
- Use exact real values inline — no tables, no placeholder tokens.
- Domain concept names in caps (plain text) or bold (md); actual values quoted or italicized (md).
- Cover at least one happy path, one failure or rejection, and edge cases where the story implies them.
